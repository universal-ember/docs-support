{
  "name": "root",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git@github.com:universal-ember/docs-support.git"
  },
  "scripts": {
    "build": "pnpm --filter '*' build",
    "lint": "pnpm --filter '*' lint",
    "test": "pnpm --filter '*' test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "release-plan": "^0.17.0"
  },
  "pnpm": {
    "overrides": {
      "@glint/template": "1.5.2"
    }
  },
  "packageManager": "pnpm@10.15.1"
}
